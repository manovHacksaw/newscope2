import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.ibb.co', 'cdn.intuji.com', 'res.cloudinary.com'], // Add more domains if needed
  },
}
  ;

export default nextConfig;
